{
  "db": {
    "name": "db",
    "connector": "memory"
  },
  "mysqlds": {
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "url": "",
    "database": "${DB_NAME}",
    "password": "${DB_PWD}",
    "name": "mysqlds",
    "user": "${DB_USERNAME}",
    "connector": "mysql"
  }
}
